###1.使用路线（怎么跑起来的）

cd D:\research_demo\sasrec_smoke_demo → conda activate myenv → python sasrec_demo.py

###2.Conda 环境、Python 和 PyTorch 版本

在命令行输入：
conda activate lesson03
python --version           # Python 版本
python -c "import torch; print(torch.__version__)"  # PyTorch 版本 

###3.GPU 型号和 CUDA 检查结果（因为你装的是 CPU 版）

执行：python -c "import torch; print(torch.cuda.is_available())"
输出结果为 False，说明是纯 CPU 跑。

###4.运行命令、日志和输出位置（直接把截图或日志贴进去）

命令：python sasrec_demo.py
输出位置：D:\research_demo\sasrec_smoke_demo\outputs\run_20260715_*
![[1988a8026beff341196866314e07dc38.png]]
修改为200 epochs 并运行输出结果：
![[Pasted image 20260715213412.png]]

###5.一个问题及排查过程

问题：运行时报错 can't open file。
排查：发现 sasrec_demo.py 在子文件夹内，执行 cd sasrec_smoke_demo 后解决。